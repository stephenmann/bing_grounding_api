Turn this sample code into a simple web application. Minimize the number of dependent components used to keep the demo as simple as possible.

Applicaton features:

1. Text box for search query styled like a search engine search box with a "Search" button to initate the search.
2. Display the results in two way:
    2a. For each result, display the result in a stylized manner simuliar to a Bing search result with the Title first that is also a hyperlink to the cited URL, then the text summary below.
    2b. Add an additional display area below the styled results that show the raw JSON response from the API call in a readable JSON format and stylzied like code in an IDE.